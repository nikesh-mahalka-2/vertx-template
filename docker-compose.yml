version: '3'

services:
  dbserver:
    image: store/oracle/database-enterprise:12.2.0.1-slim
    ports:
      - "1521:1521"
  appserver:
    build: .
    # command: sleep 10000
    depends_on:
      - dbserver


